📌 STATUS CLOUD — 2026-06-04
✅ Concluído nesta sessão
app/helpers/Tenant.php — implementação completa do método listarControladores(\PDO $pdo, ?int $empresaId = null): array
Filtro multi-tenant aplicado (master/escopado/fail-safe)
Soft-delete respeitado (c.deleted_at IS NULL)
Status online calculado em tempo real via comparação com telemetria_5min
3 ajustes técnicos pós-revisão aplicados pelo ATGY com sucesso:
✅ Cast defensivo (int) em \App\Config\Constantes::TIMEOUT_TELEMETRIA_ONLINE_SEG
✅ Troca de NOW() por UTC_TIMESTAMP() (timezone-safe na comparação UTC↔UTC)
✅ Refactor de 2 subqueries EXISTS por 1 LEFT JOIN agregado em telemetria_5min (uma única passagem na tabela, apoiada no índice idx_controlador_ts_desc)
PHPDoc completo com shape tipado do retorno + histórico de versão atualizado para v1.1.0
Script temporário de validação criado: _teste_listar_controladores.php (na raiz do projeto, não em public_html/)
🧠 Decisões técnicas
UTC_TIMESTAMP() em vez de NOW() — NOW() depende do time_zone da sessão MySQL. Em servidor BR (default Laragon), causaria defasagem de 3h e marcaria controladores online como offline. UTC_TIMESTAMP() é absoluto.
LEFT JOIN agregado em vez de EXISTS por linha — com 200+ controladores e telemetria_5min crescendo, subqueries por linha degradariam rápido. Agregação única escala melhor.
status_online cast pra (bool) no PHP — MySQL retorna 0/1 na comparação; padronizamos pro frontend receber true/false.
Convenção de caminho do projeto: Laragon serve direto da raiz (C:/laragon/www/monitor.aeonium.com.br/). Não existe public_html/ nesse projeto. ⚠️ Anotar como contexto fixo pras próximas sessões.
📂 Arquivos tocados
app/helpers/Tenant.php — adicionado método listarControladores() + bump pra v1.1.0
_teste_listar_controladores.php (raiz) — script temporário de validação end-to-end ⚠️ REMOVER APÓS VALIDAÇÃO
🔄 Impactos / dependências afetadas
Nenhum endpoint existente foi alterado.
Próximas telas/endpoints que listem controladores (dashboard, seleção, resumo) já podem consumir o método novo com status_online + ultima_leitura_utc prontos.
Dependência implícita: classe \App\Config\Constantes precisa existir e expor a constante TIMEOUT_TELEMETRIA_ONLINE_SEG.
🔗 Impacto no firmware
Nenhum nesta sessão. Apenas leitura de telemetria_5min que o firmware já popula.
▶️ Próxima ação (retomada)
Validar end-to-end o _teste_listar_controladores.php.

Status atual do teste: TRAVADO em retorno {"erro": "Não autorizado"} em todos os cenários executados (ver seção abaixo).

Plano de retomada:

Fernando faz login normal em https://monitor.aeonium.com.br/
Na mesma aba, abre https://monitor.aeonium.com.br/_teste_listar_controladores.php
Se ainda retornar {"erro": "Não autorizado"} → colar no chat:
Conteúdo de index.php (raiz)
Conteúdo do bootstrap/autoload do projeto
Conteúdo de um endpoint que funciona (ex: api/energia/dia.php)
Com esses arquivos, montar prompt definitivo pro ATGY ajustar o script de teste pra herdar corretamente sessão + $pdo do bootstrap real
Rodar os 4 cenários de validação (master sem filtro, master com filtro, escopado tentando burlar, deslogado)
Se passar → remover o script de teste e fechar Entrega 1.1
Avançar para Entrega 1.2: migrations de ciclos_faturamento + ALTERs em controladores
⚠️ Pontos de atenção
🔴 Resultado dos testes desta sessão — TODOS retornaram {"erro": "Não autorizado"}



#	Cenário	URL testada	Resultado	Esperado
1	Master sem filtro	/_teste_listar_controladores.php	❌ {"erro": "Não autorizado"}	JSON com todos controladores
2	Master com filtro	/_teste_listar_controladores.php?empresa_id=2	❌ {"erro": "Não autorizado"}	JSON filtrado pela empresa 2
3	Escopado burlando	/_teste_listar_controladores.php?empresa_id=999	❌ {"erro": "Não autorizado"}	JSON só com empresa do user logado
4	Deslogado	/_teste_listar_controladores.php	❌ {"erro": "Não autorizado"}	HTTP 401 (esperado nesse caso)
🔍 Diagnóstico do problema
O retorno {"erro": "Não autorizado"} (com acento, maiúscula, formato curto) NÃO veio do nosso script. Nosso script retornaria:

json


{"erro": "nao_autenticado", "mensagem": "Faca login antes de testar."}
Conclusão: algum middleware/guard global do projeto está interceptando o request ANTES do nosso código executar. Pode ser:

Hipótese 1 (mais provável): Fernando não estava logado no navegador na hora do teste. O middleware do projeto barrou geral.
Hipótese 2: Existe um middleware de autenticação acionado pelo bootstrap/autoload, e o session_start() puro do script de teste não está sintonizado com o esquema de sessão do projeto (nome de cookie diferente, path customizado, regenerate_id, etc.).
📋 Para distinguir as hipóteses na retomada
Se logar primeiro e depois testar na mesma aba já resolver → era Hipótese 1, seguimos direto pros 4 cenários.
Se mesmo logado continuar barrando → era Hipótese 2, precisamos do bootstrap real do projeto pra ajustar o script.
🧹 Pendência de limpeza
Arquivo _teste_listar_controladores.php na raiz é temporário. Não deixar em produção. Remover assim que a validação passar.
Bom papo, Fernando! Quando voltar, é só colar esse status que eu retomo do ponto exato. 👋🔧