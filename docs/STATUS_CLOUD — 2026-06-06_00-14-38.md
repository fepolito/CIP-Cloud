🫡 Entendido, Fernando! Boa saída. Deixei tudo amarrado pra retomarmos exatamente de onde paramos.

markdown


## 📌 STATUS CLOUD — 2026-06-06

### ✅ Concluído nesta sessão
- **Prompt F1.A enviado ao ATGY** — endpoints `api/dashboard/resumo_dia.php` 
  e `api/dashboard/resumo_mes.php` criados respeitando contrato:
  - Tenant não modificado
  - Timezone obtido da tabela `controladores` (não via parâmetro)
  - Slots battery-ready reservados (`bateria_carga_kwh`, `bateria_descarga_kwh`, 
    `tem_bateria: false`)
  - Resposta padronizada `{status, dados, mensagem}`
  - `declare(strict_types=1)` + PSR-12 + comentários pt-BR
- **Schema real da telemetria descoberto** — tabela é `telemetria_min` 
  (não `energia`). Mapeamento completo de colunas levantado.
- **Diagnóstico do erro 500** iniciado via análise do `php_error.log`.

### 🧠 Decisões técnicas
- **Tabela de telemetria confirmada:** `telemetria_min` (não `energia`)
- **Mapeamento de colunas real:**
  - `geracao_kwh` (chute) → `energia_geracao_kwh` (real)
  - `injetada_kwh` (chute) → `energia_exportada_kwh` (real)
  - `importada_kwh` (chute) → `energia_importada_kwh` (real)
  - `criado_em_utc` (chute) → `timestamp_utc` (real)
- **Campos valiosos descobertos** (para uso futuro):
  - `geracao_origem` (enum: `inversor` | `estimado` | `api_externa` | `indisponivel`) 
    → mapear para `fonte_dado` no payload
  - `qualidade_dado` (tinyint) → flag de confiabilidade
  - `power_injected_w`, `is_exporting`, `direction_valid` → telemetria instantânea
  - `temperatura_inversor_c`, contadores Modbus → telemetria de saúde (feature futura)
- **Mapeamento `geracao_origem` → `fonte_dado`:**
inversor → 'cip' api_externa → 'solis_api' estimado → 'cache' indisponivel → 'sem_dados'



- **Fórmula de consumo confirmada:** 
`consumo = geracao - injetada + importada` (com proteção para arredondamento negativo)

### 📂 Arquivos tocados
- `api/dashboard/resumo_dia.php` — **CRIADO pelo ATGY (v1.0.0)** — 
⚠️ contém schema errado (`energia` + colunas inexistentes), precisa refazer
- `api/dashboard/resumo_mes.php` — **CRIADO pelo ATGY (v1.0.0)** — 
⚠️ mesma situação, precisa refazer

### 🔄 Impactos / dependências afetadas
- Nenhum arquivo existente foi modificado nesta sessão
- `Tenant.php` permanece intocado (conforme regra)
- `snapshot.php` continua funcional (referência viva da estrutura correta)

### 🔗 Impacto no firmware
- ❌ Nenhum — sessão 100% cloud

### ❌ PENDÊNCIA CRÍTICA — F1.A NÃO ESTÁ FUNCIONAL
Endpoints retornam erro. Investigação parou no log do PHP que revelou:

**1. Bug ANTIGO descoberto (de 02-Abril-2026, não é da nossa sessão):**
 - `api/index.php` linha 46 tenta `require_once` em:
   `api/midleware/auth.php` ← **TYPO** (falta um 'd')
 - Caminho correto provavelmente: `api/middleware/auth.php`
 - Esse bug está há ~2 meses sem ninguém perceber

**2. Erro 500 atual dos NOSSOS endpoints:**
 - Pode ser causado por (a) o front controller quebrado interceptando 
   as rotas novas, OU (b) o schema errado dos endpoints (tabela `energia` 
   que não existe), OU (c) ambos
 - **NÃO foi isolado** — falta gerar log fresco para confirmar

### 🎯 RETOMADA — O QUE FAZER NA PRÓXIMA SESSÃO

**Fernando NÃO executou nenhum dos passos abaixo. Pedir novamente:**

#### Passo 1 — Listar estrutura da pasta api/
dir C:\laragon\www\monitor.aeonium.com.br\public_html\api
dir C:\laragon\www\monitor.aeonium.com.br\public_html\api\middleware
dir C:\laragon\www\monitor.aeonium.com.br\public_html\api\midleware\



Descobrir: qual a grafia real da pasta (middleware ou midleware), 
se existe `auth.php`, e se existe `api/index.php` (front controller).

#### Passo 2 — Inspecionar front controller (se existir)
- Colar conteúdo do `api/index.php` linhas 1-60
- Colar conteúdo do `api/.htaccess` (raiz da api/)

#### Passo 3 — Gerar log fresco do erro 500
1. Esvaziar (ou apagar) o `php_error.log` atual
2. Acessar: `http://monitor.aeonium.com.br.test/api/dashboard/resumo_dia.php?controlador_id=3`
3. Colar as últimas linhas do log recém-geradas

#### Passo 4 — Decidir estratégia conforme cenário
| Cenário | Ação |
|---------|------|
| Front controller intercepta tudo | Corrigir typo + registrar rota dos novos endpoints |
| Endpoints rodam direto (bypass) | Aplicar correção F1.A.v2 (schema real) |
| Misto | Seguir padrão do `snapshot.php` que já funciona |

#### Passo 5 — Mandar prompt F1.A.v2 pro ATGY
Prompt pronto na mensagem anterior do CIP Cloud Copilot — substitui tabela 
`energia` por `telemetria_min` e usa colunas reais.

### ⚠️ Pontos de atenção
- ⚠️ **Bug antigo do `api/index.php` (typo "midleware") não é prioridade**, 
  mas se ele estiver interceptando rotas novas, vira bloqueador
- ⚠️ **NÃO commitar nada da F1.A** até que os endpoints retornem JSON válido
- ⚠️ Quando aplicar `display_errors` em DEV para debug, **remover antes do commit**
- ⚠️ O `snapshot.php` (que funciona) é a melhor referência viva de como 
  endpoints devem ser estruturados neste projeto — usar de modelo
- ⚠️ Tabela `telemetria_min` tem campos preciosos para features futuras 
  (saúde do inversor, qualidade de dado, telemetria instantânea) — anotado 
  para roadmap pós-F1

### ▶️ Próxima ação (na retomada)
Reabrir esta sessão dizendo "vamos retomar F1.A" e executar os Passos 1-3 
acima para finalmente isolar o erro 500 e desbloquear o commit.
📁 Salva esse bloco em docs/STATUS_CLOUD.md (sobrescrevendo o anterior ou adicionando como nova entrada — você que sabe a convenção do repo).

🍀 Boa, Fernando! Quando voltar, é só falar "retomar F1.A" que eu puxo daqui exatamente. Sessão produtiva apesar do bloqueio — descobrimos o schema real e um bug fantasma de 2 meses no caminho. 🏺