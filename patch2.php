<?php
\ = file_get_contents('C:/laragon/www/monitor.aeonium.com.br/dashboard.php');

// Try catch in aplicarDados
\ = '    function aplicarDados(p) {
      if (!p || !p.success) return;

      // Caso "vazio"';
\ = '    function aplicarDados(p) {
      if (!p || !p.success) return;
      try {
      // Caso "vazio"';

\ = '    }

    /* --------- Fetch principal --------- */';
\ = '      } catch (e) {
        console.error('."'".'[CIP] aplicarDados EXPLODIU ->'."'".', e);
        throw e;
      }
    }

    /* --------- Fetch principal --------- */';

\ = str_replace(\, \, \);
\ = str_replace(\, \, \);
file_put_contents('C:/laragon/www/monitor.aeonium.com.br/dashboard.php', \);
